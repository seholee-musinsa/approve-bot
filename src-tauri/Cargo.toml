[package]
name = "approve-bot"
version = "0.1.0"
description = "Auto-approve GitHub PRs from selected authors"
edition = "2021"

[lib]
name = "approve_bot_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-notification = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
# rustls-tls-native-roots: rustls TLS stack + OS trust store (loads corporate SASE
# MITM CA from macOS keychain). Plain rustls-tls only trusts bundled webpki-roots,
# which rejects the intercepted api.github.com cert on the corporate network.
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls-native-roots"] }
anyhow = "1"
thiserror = "1"
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
